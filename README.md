# PinPoint — Interactive Favorite Location Manager

PinPoint is a responsive React web application that allows users to add, manage, search, categorize, and interact with their favorite locations through an interactive map.

The application provides a synchronized map and sidebar experience where users can create locations, select markers, edit details, delete locations, filter and sort saved places, and persist their data using browser localStorage.

## Features

### Core Features

- Interactive map using React Leaflet and Leaflet
- OpenStreetMap integration
- Add locations by clicking on the map
- Automatic latitude and longitude capture
- Saved locations displayed in a sidebar
- Select a location from the sidebar and focus the map on it
- Search saved locations
- Edit saved locations
- Delete saved locations
- Unique ID for every saved location
- Browser localStorage persistence
- Browser geolocation / Locate Me functionality
- Marker popups
- Responsive layout for desktop and mobile
- Keyboard-accessible location cards
- Smooth map transitions and UI animations

### Optional Enhancements Implemented

- Reverse geocoding
- Location categories
- Category filtering
- Sorting
- Draggable map markers
- Marker popups
- Keyboard accessibility
- UI animations and transitions

## Technology Stack

- React 18
- Vite
- JavaScript / JSX
- Tailwind CSS
- Leaflet
- React Leaflet
- OpenStreetMap
- Nominatim Reverse Geocoding
- Lucide React
- Framer Motion
- React Router
- Browser localStorage

## Prerequisites

Make sure the following are installed:

- Node.js
- npm

Installed Versions:

```bash
node -v
npm -v